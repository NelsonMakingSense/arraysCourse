const letters = ['a', 'b', 'c']

for (let index = 0; index < letters.length; index++) {
   console.log(letters[index])    
}

// el mismo ciclo con una funcion de array 
letters.forEach(element => {
    console.log(element)
});